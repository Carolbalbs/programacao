#include <stdlib.h>
#include <stdio.h>

int main (){

    int contador = 1; 

    while (contador <= 50)
    {
        printf("%i \n", contador);
        contador = contador + 2;
    }
    

    return 0;
}