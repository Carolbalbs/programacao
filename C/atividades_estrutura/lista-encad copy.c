#include <stdio.h>
#include <stdlib.h>
/*
Elaborar um programa C para gerenciar uma lista de espera para uma clínica.
 
A lista deve ser implementada como uma lista 
linear duplamente encadeada, sendo ordenada
por prioridade do cliente e por ordem de chegada.

Um novo cliente que chega na clínica deve ter
seu nome inserido na lista, juntamente com sua
prioridade, além de um número de identificação.

O atendente irá digitar o nome do cliente e a prioridade 
(o número de identificação pode ser gerado pelo sistema 
ou informado pelo usuário do sistema). 

A prioridade será um número inteiro entre 1 e 3.

O sistema deve inserir um novo nó na lista, 
em ordem decrescente de prioridade, sendo o 
novo nó o último de sua prioridade.

Para o atendimento, será sempre selecionado 
o primeiro nó da lista, como em uma fila, 
o que será removido da lista.

*/
int main (){

return 0;
}

void insert(){
    pont = busca(int x)
    if(pont->chave != x){
        anterior = pont-> ant
        ocupar (ptaux)
        ptaux->chave
        ptaux->ant
        ptaux->prox
        anterior->prox = ptaux 
        pont->ant = ptaux
        if (id < ptlista->chave)
        {
            ptlista-> = pont
      }
        else
        {
            sem elemento
        }
        
    }

}
void remove(){
    if (pont ->chave)
    {
        anterior = pont ->ant
        proximo = pont -> prox
        anterior->prox = proximo 
        proximo-> ant = anterior
        if (pont = ptlista)
        {
            ptlista = proximo
            desocupar(pont)
        }
        
    }else
    {
        elemento nao achado
        
    }
    
    
}
